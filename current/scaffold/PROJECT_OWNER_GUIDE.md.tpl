# {{PROJECT_NAME}} 项目所有者指南

> 当前项目使用 Standard 治理。本模式可能由 Lite 在同一项目目录中原地升级而来，原需求编号、历史和代码保持不变。
> 若由 Lite 升级，必须先完成一次 Standard 基线审计；审计通过前项目经理主智能体不能继续开发批次或正式发布。

## 你负责的事情

1. 与项目经理主智能体讨论项目、需求、页面、流程和最终效果；
2. 对产品、架构和开发规划作确认；
3. 对高风险操作明确授权或拒绝；
4. 实际体验软件并反馈通过、未通过或需要调整；
5. 在发布和结案前确认最终需求验收与交付物。

## 你不需要做的事情

- 不需要输入项目维护命令；
- 不需要指定必须使用哪一种代码工具；
- 不需要手工维护需求编号、修订历史和追踪矩阵；
- 不需要提醒智能体更新哪些文档；
- 不需要维护批次、日志、摘要或交接文件；
- 不需要在新窗口复制全部聊天记录。

## 需求如何保存

- 每项正式需求拥有永久编号，如 `R-027`；
- 需求变化时自动形成新版本，如 `R-027.3`；
- 当前需求文档只显示最新版本；
- 旧版本保存在需求修订台账；
- 实现、测试和人工验收通过需求追踪矩阵关联；
- 最终验收只对照发布基线中冻结的当前版本；
- 旧窗口提交过期版本时会被拒绝，必须重新读取最新需求。

## 验收中提出调整

直接自然表达即可。项目经理主智能体应先登记变更并修订需求版本，再更新验收与追踪信息，之后才分派代码执行器修改实现。

## 项目结束时

项目经理主智能体应根据不可覆盖的发布基线、实际实现、测试和人工验收生成客户使用说明、帮助文档、最终验收报告、交付清单、发布说明和结案报告。项目内交付物会记录 SHA-256。只有 `ready` 交付物才能验收；验收后交付内容发生变化时必须重新验收。你只需要审核实际内容并确认。结案后不得修改交付清单，项目及该发布版本永久冻结；继续开发时需要你明确提供原结案版本、更高的新开发版本、重开原因和授权说明。

## 切换新窗口

只需要说：

```text
读取项目根目录 START_HERE.md，按最低够用的上下文级别和正确角色接管并继续。
```

## 重要边界

本模板是流程级约束和本地可追溯机制，不是不可绕过的操作系统沙箱。重要项目仍应结合远端审查、备份和人工验收。
