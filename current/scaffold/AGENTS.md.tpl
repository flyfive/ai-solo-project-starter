# AGENTS.md

## 项目

- 名称：{{PROJECT_NAME}}
- 类型：{{PROJECT_PROFILE}}
- 治理模式：{{GOVERNANCE_MODE_LABEL}}（`{{GOVERNANCE_MODE}}`）
- 默认语言：中文
- 工作方式：用户零命令、工具无关、角色驱动

## 接管前置

1. 先读取 `START_HERE.md`、本规则、`docs/CURRENT_STATE.md` 和 ai-resume 生成的 context.json；首次承担角色或配置变化时读取 PROJECT.toml。
2. 根据 `[governance].mode` 使用 Lite 或 Standard 规则，不得把两者混为一谈。
3. 同一项目同一时间只允许一个活跃的项目经理主智能体。
4. 具体工具名称不构成项目事实，不得写入核心规则作为依赖。
5. Standard 首次承担角色或规则变化时读取角色边界和自动化契约；日常遵守 AI_EXECUTION_RULES 与 Context Loading Protocol，按活动 R/C/DEC 引用取正文。

## 全模式共同规则

1. 用户提出产品行为调整后，必须先登记和澄清，不得直接用代码覆盖旧需求。
2. 正式需求使用永久编号 `R-NNN.n`；语义变化递增修订号，纯实现与测试证据不递增。
3. 当前需求以 `docs/PRODUCT_REQUIREMENTS.md` 为准；旧语义保存在 `docs/REQUIREMENT_CHANGELOG.md`。
4. `link` 只能补充实现、测试和验收追踪证据；产品语义变化必须走 `revise`。
5. 先更新需求、验收、当前状态或决定等权威事实，再修改实现。
6. 提交必须通过 `.githooks/pre-commit`；禁止使用 `--no-verify` 绕过。
7. 不得覆盖、删除、回滚或纳入任务范围外的工作区修改。
8. 不得虚构测试、授权、文件修改、人工验收或外部执行结果。
9. 高风险操作必须遵守 `PROJECT.toml` 授权边界。
10. 缺少本地文件、终端、Git 或必要权限时必须如实说明。

## Lite 极简治理

Lite 用于预计一至三天、单执行器、自用且没有正式客户交付的小项目。

1. 先读 Hot Context，按需提取当前需求和 AC、相关决定及代码；不增加 Standard 索引和父批文档。
2. 默认单执行器顺序开发；禁止并行代码执行和集成审核流程。
3. 保留需求编号、完整修订历史、当前状态、决定、月度日志和 Git。
4. 测试强度与风险相称，但声称完成的测试必须真实通过。
5. Lite 不允许创建正式发布基线、客户交付、最终验收或项目结案。
6. 出现多阶段、并行执行、正式交付、复杂架构、敏感数据或六项以上正式需求时，应建议升级 Standard。
7. 架构、阶段规划、角色、执行规则、永久避坑或交付规则发生变化前，必须先升级 Standard。
8. Lite 是轻量需求与执行规划，不得宣称已具备 Standard 式完整架构、阶段规划或正式交付闭环。

## Standard 标准治理

Standard 用于持续数周或数月、多模块、多阶段、多人／多执行器或有正式交付目标的项目。

1. 每次接管先检查未关闭变更和活动批次，并按固定优先级恢复。
2. 需求、验收、架构、规划、角色边界、执行规则或永久体验变化时，必须先更新对应权威文档。
3. 未关闭变更不得关闭批次、阶段或进入下一阶段。
4. 并行代码执行必须经过范围隔离和集成审核；必需测试必须全部为 `pass`。
5. 代码执行器不得自行修改共享语义文档或改变项目基线。
6. 发布、最终验收和结案必须基于有效发布基线与完整需求追踪。
7. 客户文档不得暴露内部智能体、批次、Hook、运行 JSON、敏感路径或未确认信息。
8. 已结案发布版本永久冻结；正式交付物必须使用项目根目录内相对路径。

## Lite 升级 Standard

1. 升级是同一项目内的治理能力激活，不是模板跨版本迁移。
2. 只能 `lite → standard`，不得降级。
3. 必须由项目所有者授权；正常情况先完成活动批次和未关闭变更。
4. 升级保留原项目目录、Git 历史、需求编号、需求修订和当前实现。
5. 缺少的 Standard 文档通过事务一次生成；失败时全部回滚。
6. 已记录为 `BLOCKED/PARTIAL` 的 Lite 批次可以通过正式挂起入口保留未完成内容、测试状态、变更和 Git 指纹；不得伪造 PASS。
7. 升级同步更新入口文档、context、handoff 和项目记忆，并进入 Standard 基线待审计状态。
8. 基线审计必须核对当前代码、需求、架构、开发计划和项目记忆；通过前禁止继续批次或发布，挂起批次在通过后恢复。
9. 隐藏 Standard 模板包必须通过整体和逐文件 SHA-256 校验。

## 记忆与容量控制

- 同一窗口优先使用当前对话记忆，不得每轮完整重读项目文档。
- 已读取且未变化的文件无需重复加载；变化只影响关联任务，索引过期需刷新，相关活动上下文需 PM 重读确认。
- 历史、完整测试/终端/浏览器证据永久保存，默认不读；先看摘要，必要时按明确原因检索。
- 索引与摘要不得替代正式正文；多个正式事实冲突时停止对应实现并先解决。
- `CURRENT_STATE.md` 只保存当前快照；历史进入月度日志、决定和 Git。
- Standard 的 `PROJECT_MEMORY.md` 只保存低成本恢复所需的当前关键事实。
- `.ai/runtime/` 只保存活动事项并覆盖更新。
- 能机器化的规则优先使用测试、类型、约束或 Hook。

## 边界

本模板是流程、状态和审计层约束，不是操作系统安全沙箱。拥有完整权限的执行环境理论上仍可绕过本地工具；重要项目应使用 Standard，并增加远端 CI、分支保护、独立备份和人工发布复核。

## 任务与结束约束

- 先遵守正式需求和架构，再参考少量实际代码/测试；无参考说明原因。仅调查不启动实现、不把想法当正式需求；小改动沿用批次，不能以行数少或“快速”绕过风险、Change、文档优先、上下文失效、升级审计和人工验收。
- PM 按需用 task_contract 声明最小适用路径及本批条件；结束以 checks 和证据核对本批 AC，fix 核对原症状，feature 核对入口连接。缺失证据不称通过，工程通过不替代人工验收，语义变化仍走 Change/revise。
- 排错才用 debug_checkpoint；只读 manifest 中最新检查点和必要引用，旧尝试/原始日志保留且默认不读。成功只返回摘要；没有排错任务不加载排错说明。
- 字段细节按任务读取 README 的“任务字段与结束核对”；排错时再读“排错字段”。已经读过且未变化不重复读。ai-resume JSON 与 context.json 二选一，HANDOFF 是替代导航，不叠加读取。

测试设施/调查任务按需读 README 对应合同与止损章节；owner_decision_required 时停止自动运行、改版和扩展。不得把兼容旧输入当成已取得新保障。
