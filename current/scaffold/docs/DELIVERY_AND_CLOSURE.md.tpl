# 客户交付、最终验收与项目结案规则

## 1. 生成依据

客户交付材料不得只根据早期需求草案生成，必须基于：

```text
发布基线中的当前需求版本
＋ 需求追踪矩阵
＋ 实际实现与测试证据
＋ 项目所有者人工验收
＋ 当前发布包和运行环境
```

## 2. 发布基线

发布前由项目经理主智能体冻结一个发布基线。基线至少记录：

- 发布版本和不可复用的发布基线编号；
- 当前生效需求版本清单；
- 需求、验收和追踪文档哈希；
- 原始基线 Git 提交及已提交实现树哈希；
- 完整审计结果；
- 未完成或已批准移出范围事项；
- 创建时间和复核角色。

基线不得覆盖。同一未结案版本需要重新冻结时，必须显式替代旧基线并保留旧记录；创建和替代以跨文件事务完成，任何失败都恢复基线、索引、状态和日志。当前需求、验收、追踪关系、Git 历史关系或已提交实现内容变化后，旧基线自动失效；仅治理与交付文档的后续提交不会无意义地废弃源码基线。

进入 `closed` 状态的基线和发布版本永久冻结，不能覆盖、替代、重建、删除或复用。项目所有者显式重开时必须声明最近结案版本和更高的新开发版本；后续基线只允许使用锁定的新版本。

## 3. 默认客户交付文档

发布基线形成后，项目经理主智能体按需生成：

- `docs/delivery/<发布键>/<基线编号>/USER_MANUAL.md`：用户使用说明；
- `HELP_AND_FAQ.md`：常见问题和故障处理；
- `FINAL_ACCEPTANCE_REPORT.md`：逐项需求验收报告；
- `PROJECT_CLOSURE_REPORT.md`：项目目标、范围、变更和结论；
- `DELIVERY_CHECKLIST.md`：安装包、源码、配置、文档等交付物；
- `RELEASE_NOTES.md`：当前版本新增、调整、修复和限制。

正式交付物状态使用 `draft`、`pending`、`ready`。只有 `ready` 可以进入项目所有者最终验收和结案。项目所有者验收后，交付物或交付文档发生任何重新生成、替换或内容变化，都必须重新验收。

## 4. 内外文档隔离

客户文档不得直接暴露：

- 智能体角色和内部任务分派；
- 批次运行 JSON、Git Hook 和临时目录；
- 内部争议过程、被否决方案和无关技术日志；
- 凭据、密钥和敏感路径；
- 未经确认的功能、计划或测试结果。

内部项目文档保存在 `docs/`，客户材料保存在 `docs/delivery/`。

## 5. 文档真实性

用户手册必须核对最终界面、菜单、按钮和实际操作路径。生成器可以建立结构和引用需求，但不能凭需求文本猜测最终界面。

客户交付验证必须检查：

- 不存在 `待补充`、`TODO`、`TBD` 等占位内容；
- 最终验收报告覆盖发布基线中的全部需求；
- 用户手册覆盖所有客户可见功能；
- 客户文档不含内部角色和运行机制；
- 交付清单中的文件真实存在；
- 交付物全部使用项目根目录内的相对路径；绝对路径、Windows 盘符、UNC、`..`、符号链接和目录链接均不能逃逸项目根目录；
- 交付物类型、SHA-256、大小和文件数量与验证记录一致；
- 源码基线后生成的未跟踪构建产物必须显式登记为交付物，不能借此忽略其他实现变化；
- 客户文档关联的发布基线编号、原始基线提交关系和实现树未失效；
- 已知限制与发布说明一致。

## 6. 项目结案门禁

只有同时满足以下条件，项目才能标记结案：

1. 无活动批次和未关闭变更；
2. 发布基线有效；
3. 客户交付文档验证通过；
4. 项目所有者最终验收通过；
5. 结案报告和交付清单已更新；
6. 项目经理主智能体完成最终复核。

结案是项目默认终态。结案过程必须具备事务性：任何报告、清单或交付验证失败时，恢复结案前状态。确需继续开发时，项目所有者必须明确提供原结案版本、新阶段、更高的新发布版本、重开原因和授权说明；已经结案的发布版本永久冻结。

结案后不得修改 `delivery_manifest.json` 或其他交付清单元数据；需要调整交付内容时必须显式重开并使用更高版本。
