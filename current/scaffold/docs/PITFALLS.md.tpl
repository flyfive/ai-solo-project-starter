# {{PROJECT_NAME}} 已知问题与避坑点

> 只保留仍可能复发、能够改变后续行为的经验。一次性故障写入月度日志。

## PIT-001：不得只在聊天中保存项目变化

- 状态：有效
- 触发场景：需求、架构、业务规则、验收标准或授权边界发生变化。
- 错误表现：聊天中确认了变化，但项目文件未更新，新窗口继续执行旧口径。
- 永久规则：批次结束前完成项目记忆写回，再通过唯一的批次结果与验收流程完成关闭。
- 验证方式：健康检查、批次状态机与提交前检查。

## 条目模板

```md
## PIT-NNN：标题

- 状态：有效 / 已解决 / 已失效
- 触发场景：
- 错误表现：
- 根因：
- 永久规则：
- 验证方式：
```
