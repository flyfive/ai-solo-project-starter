# AI 执行规则

本文件属于 Standard。角色名称为项目所有者、项目经理主智能体（PM）、审核/分析、代码执行器、集成审核；不绑定软件或模型。

1. 先读 Hot Context：START_HERE 的项目简述、AGENTS、CURRENT_STATE 和工具生成的 CONTEXT_LOAD_MANIFEST（.ai/runtime/context.json）。
2. 普通任务不默认全文重读项目。索引也只读取当前任务筛选出的行，不全文加载无限增长的索引。
3. 根据活动批次 context_refs 精确加载相关 Requirement/AC、Architecture Cxx 与 Decision；先取得正式正文再实施。
4. 未加载内容不得猜测。没有 ID 的旧任务由 PM 明确范围；不能因此自动加载全部需求。
5. 历史只在回归失败、事实/决定冲突、兼容调查、所有者追溯或明确证据引用时按 ID/路径检索。
6. 正式事实修改后刷新派生索引。相关活动上下文失效时，由 PM 重新读取、核对并显式确认刷新；刷新索引不等于自动批准旧任务继续。
7. CURRENT_STATE 表达现在是什么；开发日志表达发生过什么。旧事实进入日志/历史，不堆积进当前状态。
8. Evidence retained ≠ Evidence always loaded。所有失败、原始测试、终端和浏览器证据保留，成功只返回结构化摘要。
9. 正式定义只保存一次。需求登记表生成受校验的需求/AC/追踪视图；架构和 Decision 正文是相应权威源，摘要与索引只引用。
10. 首次基线、架构级重构、阶段/发布审计、严重事实冲突或所有者要求可完整审查；完成后恢复普通 Hot/Task 加载。
11. 新执行器按接管协议恢复当前任务，不从项目创建第一天重新学习历史。
12. Hot Context 与正式正文冲突时正文优先，阻止受影响实现并修复索引/摘要；多个正式源冲突时先由 PM 解决事实冲突。
13. 永久需求编号、真实 Change、文档优先、验收、单一活动批次、并行隔离、Git Hook、发布基线与结案冻结全部继续生效。

## 写回责任

代码执行器只修改分配范围的代码/测试并返回证据。PM 修改正式需求、架构、规划、角色与决定。工具刷新当前状态和索引；PROJECT_MEMORY 只保留跨任务有效的引用，不要求每次复制需求正文。日志保存必要原因和证据位置。

## Parent Batch / Internal Units

Standard 可选择父批+内部 U1/U2 等工作单元；Lite 继续单批顺序开发。Unit 必须明确范围、引用和局部测试，失败不得跳过，所有尝试保存。Unit 不单独正式打包、人工验收或建立发布基线。父批在全部 Unit 通过后统一回归和适用的消费者/浏览器/固定包/REPORT/CHECKS 检查；不适用须说明。风险过大的范围应拆分父批，不得用大批次掩盖风险。

## 测试与输出层级

L1 Unit/Local；L2 Module；L3 Parent Batch Regression；L4 Stage/Integration；L5 Final Release。普通修改优先 L1/L2，父批至少 L3，安全/数据完整性/Migration/高风险至少 L4；阶段和正式发布按 L4/L5 完成。实际测试清单由 PM 按风险决定，不能只改标签伪造覆盖。使用精确 context_refs、父批或提升测试级别后，项目启用分层测试契约；阶段验收和发布基线分别检查 purpose=stage 的 L4 和 purpose=release 的 L5 原始证据。旧版无扩展字段的请求保留原测试契约，兼容不等于声明获得新分层保证。

通过 ai-evidence 保存真实原始输出和 JSON summary，或导入已有浏览器 trace、SQL/构建日志等项目内文件。失败先读取失败 case/相关片段，需要时沿 raw 路径继续读取；成功不把数百条 PASS 重复返回 PM。涉及外部或高风险操作仍遵守现有授权，不因证据命令获得额外权限。

## 统计

记录返回字节/字符、读取文件和章节、上下文层级、Cold 访问、完整审查记录。无真实 API token metrics 时不得把估算写成 token 消耗。

## 任务约束与排错

遵守 AGENTS 的任务与结束约束；字段细节只按需读取 README 对应章节，不重复加载已读且未变化的正文。原风险测试与人工验收门禁不变。

## V1.9.1 验证设施边界

新 planned/高风险/L3+ 验证采用 README 的“测试设施合同”；调查/范围扩张按“调查止损与范围决定”按需读取。Candidate、Contract、Harness、Evidence 分离；旧历史不重写，已有治理门禁继续有效。完整字段不在本入口重复。
