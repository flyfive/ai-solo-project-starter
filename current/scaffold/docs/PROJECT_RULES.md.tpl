# 项目执行、角色治理、长期记忆与上下文规则

> 本文件属于 Standard 治理。直接初始化 Standard 或由 Lite 经项目所有者授权原地升级后生效。升级保留原项目目录、Git、需求编号、修订历史和代码；Standard 不允许降级 Lite。
> Lite 升级后必须完成当前代码、需求、架构、计划和项目记忆的基线审计；审计通过前不得继续批次、阶段或发布。因升级挂起的原批次仅在审计通过后恢复。

## 1. 总体原则

1. 当前对话负责高效连续工作，本地文件保存可恢复的项目事实。
2. `PROJECT_MEMORY.md` 是短摘要，不替代需求、架构等权威文档。
3. 项目协作以角色为中心，不绑定任何具体工具、模型、平台或品牌。
4. 同一项目同一时间只有一个活跃项目经理主智能体。
5. 用户反馈改变产品行为、验收、架构、阶段范围或永久体验时，必须进入待处理变更流程。
6. 文档优先：先更新并验证权威文档和项目记忆摘要，再修改实现。
7. 用户不承担固定维护命令，承担相应角色且具备本地权限的智能体自行完成。

## 2. 角色治理

角色定义以 `ROLE_BOUNDARIES.md` 为准：

- 项目所有者：产品决策、风险授权和人工验收；
- 项目经理主智能体：需求、架构、规划、任务分派、文档写回和最终项目判断；
- 分析／审核智能体：只读分析、审核和影响评估；
- 代码执行器：在分派范围内修改代码、测试并返回证据；
- 集成审核角色：整合并行结果、处理冲突和执行全量回归。

任何角色都不得使用具体工具能力作为越权理由。

## 3. 三层上下文

L1 Hot：当前简述、稳定规则、CURRENT_STATE、活动批次和筛选后的索引行。
L2 Task：按 R/C/DEC 精确提取当前正式正文、AC、相关代码与当前阶段。
L3 Cold：历史日志、旧修订、关闭的批次/Change、旧验收和 raw evidence；默认不读，按明确原因追溯。

## 4. 上下文读取协议

详细执行规则见 AI_EXECUTION_RULES，接口和接管协议见 CONTEXT_LOADING_PROTOCOL。完整审查仅首次基线、架构重构、阶段/发布审计、严重事实冲突或所有者明确要求；审查后恢复普通按需读取。PROJECT_MEMORY 是可选稳定导航，不能复制正式正文。日志不承担当前状态职责。

## 5. 权威文档

| 信息 | 权威文件 | 更新方式 |
|---|---|---|
| 角色与权限 | `ROLE_BOUNDARIES.md` | 稳定维护，变化需记录决定 |
| 快速恢复摘要 | `PROJECT_MEMORY.md` | 覆盖式压缩 |
| 当前有效需求 | `PRODUCT_REQUIREMENTS.md` | 替换为当前口径 |
| 验收条件 | `ACCEPTANCE_CRITERIA.md` | 与需求同步 |
| 当前架构 | `ARCHITECTURE.md` | 只描述当前架构 |
| 开发阶段和范围 | `DEVELOPMENT_PLAN.md` | 维护当前计划 |
| 当前进度 | `CURRENT_STATE.md` | 覆盖式更新 |
| 重要变化原因 | `DECISIONS.md` | 追加决策条目 |
| 重复性风险 | `PITFALLS.md` | 维护有效条目 |
| 历史执行 | `logs/YYYY-MM.md` | 按月追加 |

摘要和历史日志不得覆盖权威需求、架构或角色边界。

## 6. 反馈分类

- `bug_fix`：实现未符合已有需求；
- `requirement_change`：目标、功能、业务规则或范围变化；
- `acceptance_change`：完成判定变化；
- `architecture_change`：技术结构、数据口径、部署或边界变化；
- `plan_change`：阶段、批次、依赖或优先级变化；
- `ui_behavior_change`：页面、交互、跨端行为变化；
- `pitfall_rule`：需要长期防止的错误；
- `execution_rule_change`：执行或授权规则变化；
- `role_boundary_change`：角色职责、权限、文档所有权或并行边界变化。

语义变化更新对应正式源和派生索引；PROJECT_MEMORY 只在其跨任务引用实际受影响时更新，不要求复制新正文。工具刷新当前状态。

## 7. 文档优先变更协议

1. 原样保存用户反馈；
2. 计算必须更新的权威文档；
3. 记录文档哈希和实现工作区基线；
4. 由有权限角色先更新权威文档及项目记忆摘要；
5. 工具验证哈希变化，并确认实现没有抢先修改；
6. 通过后才允许代码执行器修改实现；
7. 执行测试、集成审核并提交人工验收；
8. 用户通过后关闭变更；未通过或有新调整时继续同一闭环。

如果代码先变化，必须恢复登记变更后新增的实现修改，不能靠补写文档掩盖顺序违规。

## 8. 并行执行协议

1. 并行任务必须由项目经理主智能体分派；
2. 每个执行线程有唯一任务编号和明确文件边界；
3. 共享语义文档由项目经理主智能体统一写回；
4. 分析／审核智能体默认只读；
5. 代码执行器只修改分配的代码、测试和技术实现文档；
6. 多线程结果必须经过集成审核；
7. 集成审核通过前不得关闭总批次；
8. 任何线程发现需求、架构或角色冲突时必须报告，不得自行决策。

## 9. 变更、批次和阶段

- `captured`：反馈已保存，文档尚未验证；
- `docs_ready`：文档写回通过，可实施；
- `rework_ready`：验收返工已完成文档检查；
- `awaiting_reacceptance` / `implemented`：等待用户重新验收；
- `closed`：用户验收或约定条件完成。

未关闭变更不得开始无关批次、关闭批次、通过阶段或进入下一阶段。

批次结果必须包含真实测试、记忆影响和项目经理主智能体复核。`PASS` 要求所有测试真实通过且不存在阻塞；`PARTIAL/BLOCKED` 只记录未完成执行，活动批次和关联变更保持打开。人工验收只能由项目所有者反馈单独记录，其他角色不得自报通过。

## 10. 阶段完整审计

进入下一阶段前必须满足：

1. 当前阶段人工验收通过；
2. 无活动批次和未关闭变更；
3. 完成当前阶段的完整上下文审计；
4. 审计覆盖当前需求、验收、架构、计划、角色边界、项目记忆、状态、决策与避坑；
5. 并行结果已完成集成审核；
6. 冲突已解决并记录项目经理主智能体复核。

完整审计是低频校准，不应在普通对话或普通批次重复执行。

## 11. Git Hook 与审计

- `.githooks/pre-commit` 阻止未完成文档写回的提交；
- 禁止使用 `--no-verify`；
- Hook 不是操作系统沙箱，重要项目可在远端 CI 重复检查；
- Git 不可用时严格写回检查失败。

## 12. 用户零命令契约

用户只负责描述项目、作产品决定、授权高风险操作和人工验收。智能体不得把内部固定命令转交用户；没有本地能力时必须如实说明。

## 13. 工具无关契约

- 核心文档不绑定具体工具名称；
- 任何具备所需能力的工具都可以承担某个角色；
- 更换工具不改变项目基线、角色权限和验收标准；
- 工具名称如需记录，只能保存在本地临时绑定信息中；
- 新工具必须先读取角色边界并接受现有职责，不能要求项目迁就工具。

## 14. 需求版本与追踪规则

1. 每项正式需求获得永久编号 `R-NNN`，编号创建后不得复用、重排或因删除而消失。
2. 产品语义变化递增修订号，例如 `R-027.2 → R-027.3`。
3. 只有实现位置、提交引用、测试证据、人工验收证据和发布/交付关联等纯追踪信息可以通过 `link` 更新且不递增修订号。
4. `PRODUCT_REQUIREMENTS.md` 只显示当前最新版本；旧版本进入 `REQUIREMENT_CHANGELOG.md`，并完整保存当时的验收描述与方法、用户说明、帮助说明、已知限制和备注等全部语义，不得只保存正文摘要；当前语义必须与最新快照一致。
5. 每项当前需求通过 `REQUIREMENT_TRACEABILITY_MATRIX.md` 关联验收、实现、测试、人工验收与发布版本。
6. 被延期或移出范围的需求必须保留永久编号和批准原因，不得静默删除。
7. 代码执行器只返回追踪证据，需求登记和版本修订由项目经理主智能体统一写入。
8. 需求标题、正文、行为、规则、范围、验收描述与方法、发布目标、用户说明和已知限制等语义变化必须关联真实未关闭变更，并使用最新 `expected_current_version` 通过 `revise` 递增修订号；`link` 对非允许字段整体拒绝且不得部分写入。
9. 非空 `release_target` 必须使用 ASCII 数字组成的合法 SemVer；创建、修订、健康检查和发布前检查均不得接受或静默忽略非法目标，已结案版本不能再作为新建或修订需求的目标。
10. 登记表和需求、验收、修订历史、追踪矩阵四份派生文档必须事务写入，失败时全部恢复。

## 15. 发布基线与客户交付

1. 发布基线只能在无活动批次、无未关闭变更、完整审计有效且工作区干净时创建。
2. 所有纳入当前发布范围的 active 需求必须达到 `COMPLETE`；客户可见需求还必须具有经过最终产品核对的使用说明。
3. 发布前必须分类列出本次纳入、无目标、未来版本、过期目标和历史已结案需求；无历史结案基线保护的过期目标或无法精确匹配的目标必须阻止发布。
4. 发布基线拥有不可复用编号，不得覆盖；冻结当前需求版本、范围分类、文档哈希、原始基线提交、已提交实现树和审计记录；需求事实或实现内容变化后基线失效。
5. 发布基线创建和替代采用跨文件事务；全部预检查和临时内容验证通过后才原子替换，失败时恢复基线、索引、状态和日志。
6. 已结案基线及其发布版本永久冻结。项目重开必须声明原结案版本和更高的新开发版本，后续基线只能使用锁定的新版本，不能重建或替代旧结案版本。
7. 客户文档只能根据有效发布基线、实际实现、测试和人工验收生成；交付物必须使用项目根目录内相对路径，禁止绝对路径、`..` 和链接逃逸。
8. 已有交付目录默认拒绝部分重生成；显式强制重生成时，全部客户文档、manifest、基线关联和日志必须事务刷新，且清单哈希必须与 manifest 一致。
9. 客户文档与内部项目文档分离，禁止暴露智能体角色、运行机制、敏感路径和未确认信息。
10. 最终验收和项目结案必须经过项目经理主智能体复核并采用可回滚事务。
11. 只有状态为 `ready` 的正式交付物才能进入验收和结案；`draft` 或 `pending` 必须先完成。
12. 项目所有者验收后，只要交付物或交付文档重新生成、替换或变化，必须重新进行项目所有者验收。
13. 结案后不得修改交付清单；需要继续开发时必须以更高版本显式重开。

## 16. 容量规则

- `ROLE_BOUNDARIES.md`：不超过 260 行或 35 KB，只保存稳定角色规则；
- `PROJECT_MEMORY.md`：不超过 300 行或 30 KB，删除失效摘要；
- `CURRENT_STATE.md`：只保存当前快照；
- 需求和架构接近上限时按模块拆分，主文件保留总口径与索引；
- 日志按月保存并自动分卷；
- `.ai/project_state.json` 和 `.ai/runtime/context.json` 覆盖更新；
- 临时区只保存活动事项，不按批次无限增长。

## 17. 真实性

只报告实际执行的修改、测试和结果。工具失败、权限不足、文档冲突、角色越权或审计未完成必须如实说明，不能把计划描述成事实。
