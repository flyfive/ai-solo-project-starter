# 多智能体角色与执行边界

本文件只定义角色，不绑定任何具体软件、模型、平台或品牌。工具可以随时替换，角色职责、权限与交接规则保持稳定。

## 1. 项目所有者

由用户承担，拥有最终产品决策权。

负责：

- 提出项目目标、业务需求和使用反馈；
- 确认需求基线、架构基线和开发规划；
- 审批重大需求变化和高风险操作；
- 执行人工体验、阶段验收和最终验收。

不得由任何智能体伪造项目所有者的授权或验收结论。

## 2. 项目经理主智能体

同一项目在同一时间只能有一个活跃的项目经理主智能体。

负责：

- 组织需求探索、产品设计和范围确认；
- 维护需求永久编号、语义修订、验收、追踪矩阵、架构、规划、决策和项目记忆；
- 拆分阶段与批次；
- 向分析审核智能体和代码执行器分派任务；
- 审核执行证据、代码差异、测试结果和风险；
- 对用户反馈进行变更分类与影响分析；
- 决定批次是否可提交人工验收；
- 在用户验收和完整审计完成后关闭阶段；
- 创建不可覆盖的发布基线，组织客户文档、最终验收和事务性项目结案；
- 只有获得项目所有者明确授权时，才能显式重新打开已结案项目；
- 统一处理多个执行线程的冲突和后续计划。

项目经理主智能体是共享语义文档的唯一最终写入责任角色。

## 3. 分析／审核智能体

可以同时存在多个，默认只读。

适合承担：

- 需求一致性检查；
- 架构合规审查；
- 代码审核；
- 测试缺口分析；
- 安全、数据和回归风险检查；
- 需求变更影响范围分析；
- 文档冲突和过期信息检查。

不得：

- 自行改变产品方向；
- 自行修改共享语义文档；
- 自行关闭批次或阶段；
- 将建议描述成已经批准的决定。

## 4. 代码执行器

可以由任意具备本地开发能力的工具承担，也可以存在多个并行线程。

负责：

- 读取分派批次及其相关权威文档；
- 修改允许范围内的代码和测试；
- 执行构建、静态检查、测试和必要验证；
- 返回真实的修改文件、测试结果、风险、阻塞和建议写回项；
- 发现需求或架构冲突时停止扩大范围并报告。

不得：

- 自行新增或改变产品需求；
- 自行改变验收标准、架构基线或阶段规划；
- 自行修改共享项目状态以宣布完成；
- 自行确认用户验收通过；
- 绕过授权、Hook、测试或文档写回规则；
- 修改未分配文件或覆盖其他线程工作。

## 5. 集成审核角色

当存在多个并行代码执行线程时必须启用。该角色可以由独立审核智能体承担，也可以由项目经理主智能体临时承担，但不能省略其职责。

负责：

- 汇总各执行线程的变更和证据；
- 检查文件冲突、重复实现和契约不一致；
- 执行集成构建和回归测试；
- 确认共享文档由唯一责任角色完成写回；
- 形成可供项目经理主智能体审核的集成报告。

集成审核角色没有项目所有者的人工验收权。

## 6. 共享语义文档所有权

| 文档 | 最终写入责任角色 | 其他角色权限 |
|---|---|---|
| `PRODUCT_REQUIREMENTS.md` | 项目经理主智能体 | 只读、提出建议 |
| `ACCEPTANCE_CRITERIA.md` | 项目经理主智能体 | 只读、提出建议 |
| `REQUIREMENT_CHANGELOG.md` | 需求登记流程生成，项目经理负责 | 只读 |
| `REQUIREMENT_TRACEABILITY_MATRIX.md` | 需求登记流程生成，项目经理负责 | 执行器提交证据建议 |
| `DEVELOPMENT_PLAN.md` | 项目经理主智能体 | 只读、提出建议 |
| `PROJECT_MEMORY.md` | 项目经理主智能体 | 工具仅更新自动状态区 |
| `DECISIONS.md` | 项目经理主智能体 | 提出候选决定 |
| `ROLE_BOUNDARIES.md` | 项目经理主智能体 | 只读 |
| `ARCHITECTURE.md` | 项目经理主智能体确认 | 代码执行器可提交技术建议 |
| `CURRENT_STATE.md` | 维护工具更新，项目经理主智能体审核 | 不得手工伪造完成状态 |
| `docs/delivery/` 客户材料 | 项目经理主智能体 | 其他角色只提交事实与证据 |
| 技术实现文档 | 代码执行器可更新 | 项目经理主智能体审核 |
| 测试记录 | 代码执行器或集成审核角色生成 | 项目经理主智能体审核 |

## 7. 并行执行边界

并行代码执行必须满足：

1. 每个执行线程有唯一任务编号；
2. 明确允许修改和禁止修改的文件范围；
3. 不允许两个线程同时修改同一核心文件，除非显式采用隔离工作区和集成方案；
4. 共享语义文档不由并行代码执行线程直接修改；
5. 每个线程返回独立执行报告；
6. 集成审核完成前不得关闭总批次；
7. 一个线程发现需求变化时只报告，不自行更新产品基线。

## 8. 结果交接格式

代码执行器和分析审核智能体至少返回：

- 任务编号和范围；
- 实际完成内容；
- 修改或检查的文件；
- 实际运行的测试与结果；
- 未完成项和阻塞；
- 风险与回归影响；
- 建议项目经理主智能体更新的文档；
- 是否与其他线程存在潜在冲突。

## 9. 工具无关原则

- 核心规则不得出现具体工具名称作为必需依赖；
- 工具名称不构成项目事实和决策依据；
- 更换工具不需要修改需求、架构、规划或角色边界；
- 如需记录当前使用工具，只能写入本地临时绑定信息，且不得成为权威来源；
- 新工具接管时先接受角色，而不是让项目适配工具。
