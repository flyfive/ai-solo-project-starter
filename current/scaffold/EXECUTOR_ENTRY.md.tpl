# 代码执行器接管入口

本文件适用于任何能够读取本地文件、修改代码、运行命令和执行测试的代码执行器。

开始任务先读取 START_HERE、AGENTS、CURRENT_STATE 与 ai-resume 生成的 CONTEXT_LOAD_MANIFEST。首次承担角色或规则变化时阅读 ROLE_BOUNDARIES 和 AI_EXECUTION_RULES。随后按活动批次 context_refs 读取相关 Requirement+AC、Architecture Cxx、Decision 正文与分配代码。

索引过期先刷新；相关任务正文已变则报告 PM 重新核对并确认，不依赖旧摘要继续。历史日志、旧验收和原始测试输出默认不读，只有明确失败/冲突/兼容/追溯原因时按 ID/路径读取。不得猜测未加载内容。

## 代码执行器职责

- 只执行项目经理主智能体已经明确分派的批次；
- 修改代码、测试和必要的技术实现文档；
- 返回真实的修改文件、测试结果、风险和阻塞；
- 发现需求、架构或阶段范围冲突时停止扩大修改并报告；
- 不得自行改变产品需求、验收标准、架构基线或开发路线；
- 不得自行创建、修订或重排 `R-NNN.n` 需求版本；
- 完成实现后必须返回可定位的实现文件、测试证据和验收建议，由项目经理主智能体写入追踪矩阵；
- 不得自行宣布人工验收通过、批次关闭或阶段完成；
- 不得覆盖其他执行线程或非本批工作区修改；
- 并行执行时必须遵守文件范围和集成边界。

具体使用哪一种软件、模型或开发环境不属于项目权威规则。工具可以替换，角色职责保持不变。
