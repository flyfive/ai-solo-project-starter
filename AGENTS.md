# 工作区维护规则

先读根 START_HERE.md。本工作区维护长期自用 AI 开发辅助工具；有问题再修复、有需要再优化，准备公开分享，但不作为商业产品或服务，不承诺固定更新、响应时间或全面兼容。

1. 固定工作区 当前仓库根目录、固定同一个 Work 维护项目。current 已建立并验证，是唯一日常修改和使用入口；来源 V1.9.0 位于 versions，冻结保留。当前源码版本 1.9.2。
2. 日常维护一份 current 源码，不默认每次复制版本目录，不把历史快照直接改作工作副本，不继续扩展目录管理机制。小改动不必升版或发布；版本用于追踪与回退。
3. 根目录本地 Git 追踪 current、根入口/维护规则、必要说明与工作流；私人记录仅本地保留；current 不建嵌套仓库。versions、releases、validation、原始迁移证据及运行缓存由 Git 忽略，磁盘文件保留。暂存前检查清单，不安装 scaffold 的业务 Hook，不改全局 Git 身份，不默认建远端或推送。
4. 只有一个活跃项目经理维护当前事实，交接时明确负责人；角色名保持工具无关。优先用现有入口和维护记录简短记录必要需求、状态、重要决定、Git 或备份、真实测试与兼容性检查，不机械铺设文档体系。
5. 普通修复按风险执行相关测试；重要调整再做完整回归和集中确认。测试用正式 Python 入口和 validation 下独立输出目录；保留失败证据和预期警告，未运行或环境受限如实报告，不放宽断言、不将历史通过冒充当前通过。
6. 默认先读工作区入口，再按任务读取 current 相关文档源码；不默认全文读取所有历史版本、日志和原始证据。scaffold 的业务项目初始状态不是本维护项目真实状态。
7. 模板实际路径为 `current/`。禁止在工作区根目录或 current 内初始化业务项目。validation 下样例只作测试，不冒充业务项目。
8. 保留工具为其他项目提供的 Lite/Standard、需求管理、验收追踪及发布交付能力，不新增第三种模式；工具自身不默认要求打包、客户交付文档、最终交付验收、项目结案或升版后重开。
9. 历史版本、报告、包、校验文件与证据保持原样，旧路径仅按需从本地迁移映射定位，公开使用不得依赖私人资料。新操作用当前实际路径；确需打包时显式输出 releases，不覆盖历史包。不因更高版本目录自动切换可用基线。
10. 不删除旧副本，清理等待用户确认；不使用镜像清理或盲目覆盖。不得进入或修改未授权业务项目，权限申请限具体来源。

11. 公开分享不等于允许发布：不默认创建/修改远端、推送、上传源码或扫描结果。已确认 MIT，版权署名 FeiXiaorong；第三方材料保留适用许可，后续许可变更须经所有者确认；Git忽略不清除可达历史，公开前分别检查候选文件与拟推送历史。

12. 日常仅在 main 维护并与 origin/main 同步。提交前运行 `git status --short` 和 `git diff --check`，核对实际暂存清单；涉及 scaffold、测试 fixture、本地路径或日志/证据时，另检查 tracked diff 无真实下游项目资料。私人 maintenance/validation/versions/releases 不入提交，无需新增发布仪式。
